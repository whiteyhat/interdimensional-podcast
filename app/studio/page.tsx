import { Studio } from '@/components/studio';
/** The producer console. Broadcast mode: /studio?broadcast=1&autostart=1 for an OBS browser source. */
export default function StudioPage() {
  return <Studio />;
}
