# Camera continuity and room gestures

The rendering strategy below is superseded by [native generation plus uniform 1080P scaling](2026-09-09-native-video-upscale.md). Full affine checks later identified residual vertical stretching that the original similarity-transform measurements missed.

Add Pepe's headphone adjustment and wristwatch glance, plus GigaChad's beard smoothing and shoulder adjustment. Each follows a short spoken line, returns to the reference pose, and shares the existing cooldown with tea and cigar. Choose the oldest overdue gesture for that speaker so no action starves.

The video jump is present in generated frames. The existing player and its CSS do not scale the stage or videos at cuts. SIFT/RANSAC measurements of fixed room features show about 2.2% scale change in a 1080P sample made from the 1376×768 still. Removing end conditioning leaves the framing mismatch at the next clip's start. A 768P control returns 1344×768 footage with stable geometry. Preparing the reference at that same 1344×768 canvas before 1080P generation reduces the measured change to 0.16% in the matched sample.

Normalize both conditioning stills once with aspect-preserving fit and center crop, upload them to fal, and use those same frames for the studio poster, first frame and last frame. Keep 1080P output and end-frame control. Preserve original artwork. Record the original hash and provide a repeatable preparation script. Keep stage/video sizing unchanged.

Validation: failing artifact-dimension test before normalization; new gesture routing and recurrence tests; full existing tests; TypeScript/lint; actual rendered examples and browser playback of consecutive clips. Generated motion can still vary, so measurements support the tested camera framing rather than an absolute guarantee for every future performance.

The longer cigar choreography also invited a separate camera push-in. A simpler description that keeps the lamp, desk edge and forearms visible reduced measured scaling from 23.6% to 0.62% in a controlled render, while retaining lighting, one puff and removal of both props. Other gestures no longer mention cigar props even in prohibitions: that wording introduced an unwanted cigar in one beard preview.

Final app-generated previews after both prompt changes measured 0.30% scale variation for the cigar and 0.09% for the beard gesture. Both return to their conditioned resting frame, and the beard preview has no unwanted prop. Headphones, watch and shoulders were also rendered and visually inspected.

Verification on September 9: all 59 gesture, engine and topic tests pass. Browser playback of four consecutive normalized clips kept the stage at 960×540 with no transform or console errors. Both uploaded reference files match the checked-in files byte for byte. The full suite passed 99 of 104 tests; five failures belong to the separate interaction/payment tests. Whole-project TypeScript and lint checks also encounter concurrent coin/payment work. The gesture/reference code and preparation scripts pass targeted lint. Measurements and rendered examples are saved under ignored `work/continuity/`.

Follow-up: the user still saw a bounce at cuts. Geometry alone did not address the player handoff. Instrumenting the real engine showed `onShown` firing while the incoming video still had opacity zero, releasing the outgoing blob before the replacement's visibility committed. The old check only measured video boxes and did not assert presentation/release ordering. `Player` now waits for `requestVideoFrameCallback`, commits visibility by the unique media URL, then acknowledges that commit in a layout effect. Obsolete callbacks are canceled and ignored; browsers without the frame API use ready data and two animation frames. The change applies to existing footage.

The new browser regression failed on the old player and passes 19 checks with each frame mechanism, including delayed presentation, a committed pause, blocked autoplay, late callbacks and restart with reused clip IDs. Run `node tests/browser/serve.mjs` and open the printed URL; append `?fallback=1` for the legacy path. Twelve real 1080P clips through the actual engine completed with zero empty picture frames, zero blank commits and zero acknowledgements while hidden. The 61 current engine/gesture/topic tests, TypeScript and player lint pass.
